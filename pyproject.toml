[tool.ruff]
target-version = "py313"
line-length = 100
src = ["lambda", "tests"]
extend-exclude = [".ask"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "RUF"]

[tool.ruff.lint.per-file-ignores]
"tests/*.py" = ["I001"]

[tool.ruff.format]
quote-style = "single"
